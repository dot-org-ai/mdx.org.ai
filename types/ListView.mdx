---
$type: https://mdx.org.ai/ComponentSpec
$id: https://mdx.org.ai/types/ListView
name: ListView
category: app
description: List view for displaying collections with search, filters, and pagination
semanticElement: div
outputs: [html, markdown, json, svg, png]
related: [View, DetailView, Table, Directory]

# Props
title: string
description: string?
items: T[]
columns: Column[]?
layout: 'table' | 'grid' | 'list' = 'table'
search: boolean = true
searchPlaceholder: string = 'Search...'
filters: Filter[]?
sort: SortOption[]?
defaultSort: string?
selection: 'none' | 'single' | 'multiple' = 'none'
selectedItems: string[]?
bulkActions: Action[]?
itemActions: Action[]?
pagination: Pagination?
loading: boolean = false
empty: EmptyStateProps?
onItemClick: (item: T) => void?
children: ReactNode?

# Parts
root: div
header: header
header-title: h1
header-description: p?
header-actions: div
toolbar: div
toolbar-search: div
toolbar-search-input: input
toolbar-filters: div
toolbar-filter: select | button
toolbar-sort: select
toolbar-view-toggle: div?
bulk-actions: div?
bulk-select-all: input
bulk-count: span
bulk-action: button
content: div
table: table?
table-header: thead
table-header-row: tr
table-header-cell: th
table-body: tbody
table-row: tr
table-cell: td
table-row-checkbox: input?
table-row-actions: div
grid: div?
grid-item: article
list: div?
list-item: article
pagination: nav
pagination-info: span
pagination-buttons: div
empty: div
loading: div

# CSS Variables
--list-view-toolbar-height: 56px
--list-view-row-height: 52px
--list-view-grid-columns: 4
--list-view-grid-gap: 1rem
---

# ListView

A comprehensive list view for displaying and managing collections of items.

## Structure

```
┌─────────────────────────────────────────────────────┐
│ header                                              │
│ ┌───────────────────────────────────┐ ┌───────────┐ │
│ │ title                             │ │ [+ New]   │ │
│ │ description                       │ │           │ │
│ └───────────────────────────────────┘ └───────────┘ │
├─────────────────────────────────────────────────────┤
│ toolbar                                             │
│ ┌─────────────┐ ┌────────┐ ┌────────┐ ┌──────────┐ │
│ │ 🔍 search   │ │ Status │ │ Type   │ │ Sort  ▾ │ │
│ └─────────────┘ └────────┘ └────────┘ └──────────┘ │
├─────────────────────────────────────────────────────┤
│ bulk-actions (when items selected)                  │
│ │ ☑ 3 selected │ [Delete] [Export] [Archive]       │
├─────────────────────────────────────────────────────┤
│ table                                               │
│ ┌───┬────────────┬──────────┬────────┬───────────┐ │
│ │ ☐ │ Name       │ Status   │ Date   │ Actions   │ │
│ ├───┼────────────┼──────────┼────────┼───────────┤ │
│ │ ☐ │ Item 1     │ Active   │ Jan 1  │ ⋮         │ │
│ │ ☑ │ Item 2     │ Pending  │ Jan 2  │ ⋮         │ │
│ │ ☐ │ Item 3     │ Active   │ Jan 3  │ ⋮         │ │
│ └───┴────────────┴──────────┴────────┴───────────┘ │
├─────────────────────────────────────────────────────┤
│ pagination                                          │
│ │ Showing 1-10 of 100 │    ← 1 2 3 ... 10 →        │
└─────────────────────────────────────────────────────┘
```

## Usage

```mdx
<ListView
  title="Users"
  description="Manage team members"
  items={users}
  columns={[
    { key: 'name', header: 'Name', sortable: true },
    { key: 'email', header: 'Email' },
    { key: 'role', header: 'Role', sortable: true },
    { key: 'status', header: 'Status', render: StatusBadge },
    { key: 'createdAt', header: 'Joined', sortable: true },
  ]}
  search
  searchPlaceholder="Search users..."
  filters={[
    { name: 'role', label: 'Role', options: ['Admin', 'Member', 'Guest'] },
    { name: 'status', label: 'Status', options: ['Active', 'Inactive'] },
  ]}
  sort={[
    { label: 'Newest', value: '-createdAt' },
    { label: 'Name A-Z', value: 'name' },
  ]}
  selection="multiple"
  bulkActions={[
    { label: 'Delete', action: 'delete', variant: 'destructive' },
    { label: 'Export', action: 'export' },
  ]}
  itemActions={[
    { label: 'Edit', action: 'edit' },
    { label: 'Delete', action: 'delete' },
  ]}
  pagination={{ page: 1, pageSize: 10, total: 100 }}
/>
```

## Layout Variants

```mdx
<ListView layout="table" items={items} />  // Default table
<ListView layout="grid" items={items} />   // Card grid
<ListView layout="list" items={items} />   // Simple list
```
