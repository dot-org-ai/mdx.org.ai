---
$type: https://mdx.org.ai/ComponentSpec
$id: https://mdx.org.ai/types/Features
name: Features
category: landing
description: Feature showcase section with grid of feature cards
semanticElement: section
outputs: [html, markdown, json, svg, png]
related: [Hero, LandingPage, Card, Stats]

# Props
title: string?
subtitle: string?
description: string?
items: Feature[]
layout: 'grid' | 'list' | 'alternating' | 'bento' = 'grid'
columns: 2 | 3 | 4 = 3
size: 'sm' | 'md' | 'lg' = 'md'
align: 'left' | 'center' = 'center'
iconStyle: 'default' | 'outlined' | 'filled' | 'gradient' = 'default'
children: ReactNode?

# Parts
root: section
container: div
header: header?
header-title: h2
header-subtitle: p?
header-description: p?
grid: div
feature: article
feature-icon: div?
feature-image: img?
feature-title: h3
feature-description: p
feature-link: a?

# CSS Variables
--features-grid-gap: 2rem
--features-icon-size: 48px
--features-icon-color: var(--color-primary)
--features-card-padding: 1.5rem
---

# Features

Feature showcase section displaying product features or benefits in a grid layout.

## Structure

### Grid Layout (default)

```
┌─────────────────────────────────────────────────────┐
│ root                                                │
│ ┌─────────────────────────────────────────────────┐ │
│ │ header                                          │ │
│ │ ┌─────────────────────────────────────────────┐ │ │
│ │ │              title                          │ │ │
│ │ │             subtitle                        │ │ │
│ │ └─────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ grid                                            │ │
│ │ ┌───────────┐ ┌───────────┐ ┌───────────┐       │ │
│ │ │ feature   │ │ feature   │ │ feature   │       │ │
│ │ │   🚀      │ │   ⚡      │ │   🔒      │       │ │
│ │ │  title    │ │  title    │ │  title    │       │ │
│ │ │  desc     │ │  desc     │ │  desc     │       │ │
│ │ └───────────┘ └───────────┘ └───────────┘       │ │
│ │ ┌───────────┐ ┌───────────┐ ┌───────────┐       │ │
│ │ │ feature   │ │ feature   │ │ feature   │       │ │
│ │ │   📊      │ │   🎨      │ │   🔧      │       │ │
│ │ │  title    │ │  title    │ │  title    │       │ │
│ │ │  desc     │ │  desc     │ │  desc     │       │ │
│ │ └───────────┘ └───────────┘ └───────────┘       │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### Alternating Layout

```
┌─────────────────────────────────────────────────────┐
│ ┌─────────────────────┐ ┌─────────────────────────┐ │
│ │ content             │ │ image                   │ │
│ │ title               │ │                         │ │
│ │ description         │ │                         │ │
│ └─────────────────────┘ └─────────────────────────┘ │
│ ┌─────────────────────────┐ ┌─────────────────────┐ │
│ │ image                   │ │ content             │ │
│ │                         │ │ title               │ │
│ │                         │ │ description         │ │
│ └─────────────────────────┘ └─────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

## Usage

```mdx
<Features
  title="Why choose us"
  subtitle="Everything you need to build modern apps"
  items={[
    {
      icon: 'zap',
      title: 'Lightning Fast',
      description: 'Optimized for performance with sub-second load times',
    },
    {
      icon: 'shield',
      title: 'Secure by Default',
      description: 'Enterprise-grade security with SOC2 compliance',
    },
    {
      icon: 'globe',
      title: 'Global Scale',
      description: 'Deploy to 300+ edge locations worldwide',
    },
    {
      icon: 'code',
      title: 'Developer First',
      description: 'Built by developers, for developers',
    },
    {
      icon: 'puzzle',
      title: 'Extensible',
      description: 'Integrate with your favorite tools',
    },
    {
      icon: 'heart',
      title: 'Great Support',
      description: '24/7 support from our expert team',
    },
  ]}
  columns={3}
  layout="grid"
/>
```
