---
$type: https://mdx.org.ai/ComponentSpec
$id: https://mdx.org.ai/types/View
name: View
category: app
description: Generic view container for App layouts - equivalent to Page for Sites
semanticElement: div
outputs: [html, markdown, json, svg, png]
related: [App, Layout, Page, ListView, DetailView]

# Props
title: string
description: string?
icon: string?
breadcrumbs: Breadcrumb[]?
actions: Action[]?
tabs: Tab[]?
activeTab: string?
loading: boolean = false
error: Error?
empty: EmptyStateProps?
children: ReactNode

# Parts
root: div
header: header
header-breadcrumbs: nav?
header-icon: span?
header-title: h1
header-description: p?
header-actions: div?
tabs: nav?
tab: button
tab-active: button
content: div
loading: div?
error: div?
empty: div?

# CSS Variables
--view-header-padding: 1.5rem
--view-content-padding: 1.5rem
--view-header-gap: 1rem
--view-tab-height: 48px
---

# View

Generic view container for applications. Views are to Apps what Pages are to Sites.

## Structure

```
┌─────────────────────────────────────────────────────┐
│ root                                                │
│ ┌─────────────────────────────────────────────────┐ │
│ │ header                                          │ │
│ │ ┌─────────────────────────────────────────────┐ │ │
│ │ │ breadcrumbs: Home / Users / John            │ │ │
│ │ └─────────────────────────────────────────────┘ │ │
│ │ ┌───────────────────────────────┐ ┌───────────┐ │ │
│ │ │ 👤 title                      │ │ actions   │ │ │
│ │ │    description                │ │ [+ New]   │ │ │
│ │ └───────────────────────────────┘ └───────────┘ │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ tabs (optional)                                 │ │
│ │ │ Overview │ Activity │ Settings │              │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ content                                         │ │
│ │                                                 │ │
│ │   {children}                                    │ │
│ │                                                 │ │
│ │   - or loading spinner                          │ │
│ │   - or error message                            │ │
│ │   - or empty state                              │ │
│ │                                                 │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

## Usage

```mdx
<View
  title="Users"
  description="Manage your team members"
  icon="users"
  breadcrumbs={[
    { label: 'Home', href: '/' },
    { label: 'Users' },
  ]}
  actions={[
    { label: 'Add User', icon: 'plus', action: 'create' },
    { label: 'Export', icon: 'download', variant: 'outline' },
  ]}
  tabs={[
    { id: 'all', label: 'All Users' },
    { id: 'active', label: 'Active' },
    { id: 'invited', label: 'Invited' },
  ]}
  activeTab="all"
>
  <UserTable users={users} />
</View>
```

## States

```mdx
// Loading
<View title="Users" loading>
  <UserTable />
</View>

// Error
<View title="Users" error={{ message: 'Failed to load users' }}>
  <UserTable />
</View>

// Empty
<View
  title="Users"
  empty={{
    icon: 'users',
    title: 'No users yet',
    description: 'Get started by adding your first team member',
    action: { label: 'Add User', action: 'create' },
  }}
>
  <UserTable users={[]} />
</View>
```
