---
$type: DocsPage
title: Product Page
description: Individual product detail page
screenshot: /screenshots/pages/product.png
---

# Product Page

A product detail page with images, description, and purchase options.

<Preview>
  <main>
    <nav aria-label="Breadcrumb">
      <ol>
        <li><a href="#">Home</a></li>
        <li><a href="#">Products</a></li>
        <li>Premium Headphones</li>
      </ol>
    </nav>

    <article>
      <section aria-label="Gallery">
        <figure></figure>
        <ul>
          <li aria-selected="true"></li>
          <li></li>
          <li></li>
          <li></li>
        </ul>
      </section>

      <section aria-label="Details">
        <mark aria-label="In Stock">In Stock</mark>
        <h1>Premium Wireless Headphones</h1>
        <div aria-label="Rating">
          <data value="4.9">★★★★★ 4.9</data>
          <span>(128 reviews)</span>
        </div>
        <div aria-label="Price">
          <data value="299">$299</data>
          <s>$399</s>
        </div>
        <p>Experience crystal-clear audio with our premium wireless headphones featuring active noise cancellation and 30-hour battery life.</p>

        <fieldset aria-label="Color">
          <legend>Color</legend>
          <menu>
            <li><button aria-pressed="true" aria-label="Black"></button></li>
            <li><button aria-label="White"></button></li>
            <li><button aria-label="Blue"></button></li>
          </menu>
        </fieldset>

        <fieldset aria-label="Quantity">
          <legend>Quantity</legend>
          <div>
            <button aria-label="Decrease">-</button>
            <output>1</output>
            <button aria-label="Increase">+</button>
          </div>
        </fieldset>

        <menu>
          <li><button>Add to Cart</button></li>
          <li><button aria-label="Add to wishlist">♡</button></li>
        </menu>
      </section>
    </article>
  </main>
</Preview>

## Semantic Pattern

A product page uses `<main>` with breadcrumb navigation and an `<article>` containing gallery and details sections. The pattern `main:has(> article > section[aria-label="Gallery"])` identifies it:

```html
<main>
  <nav aria-label="Breadcrumb">
    <ol>
      <li><a href="/">Home</a></li>
      <li><a href="/products">Products</a></li>
      <li>Premium Headphones</li>
    </ol>
  </nav>

  <article>
    <section aria-label="Gallery">
      <figure><img src="/product.jpg" alt="Product" /></figure>
      <ul>
        <li aria-selected="true"><img src="/thumb1.jpg" alt="" /></li>
        <li><img src="/thumb2.jpg" alt="" /></li>
      </ul>
    </section>

    <section aria-label="Details">
      <mark aria-label="In Stock">In Stock</mark>
      <h1>Premium Wireless Headphones</h1>
      <div aria-label="Rating">
        <data value="4.9">★★★★★ 4.9</data>
        <span>(128 reviews)</span>
      </div>
      <div aria-label="Price">
        <data value="299">$299</data>
        <s>$399</s>
      </div>
      <p>Experience crystal-clear audio...</p>

      <fieldset aria-label="Color">
        <legend>Color</legend>
        <menu>
          <li><button aria-pressed="true" aria-label="Black"></button></li>
          <li><button aria-label="White"></button></li>
        </menu>
      </fieldset>

      <fieldset aria-label="Quantity">
        <legend>Quantity</legend>
        <div>
          <button aria-label="Decrease">-</button>
          <output>1</output>
          <button aria-label="Increase">+</button>
        </div>
      </fieldset>

      <menu>
        <li><button>Add to Cart</button></li>
        <li><button aria-label="Add to wishlist">♡</button></li>
      </menu>
    </section>
  </article>
</main>
```

## CSS

```css
/* Product page: main with breadcrumb + article with gallery */
main:has(> article > section[aria-label="Gallery"]) {
  padding: var(--spacing-6);
}

main:has(> article > section[aria-label="Gallery"]) > nav[aria-label="Breadcrumb"] {
  font-size: var(--text-sm);
  color: var(--muted-foreground);
  margin-bottom: var(--spacing-4);
}

main:has(> article > section[aria-label="Gallery"]) > nav ol {
  display: flex;
  gap: var(--spacing-2);
  list-style: none;
  padding: 0;
}

main:has(> article > section[aria-label="Gallery"]) > nav li:not(:last-child)::after {
  content: "/";
  margin-left: var(--spacing-2);
}

main:has(> article > section[aria-label="Gallery"]) > article {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--spacing-8);
}

/* Gallery section */
section[aria-label="Gallery"] figure {
  background: var(--muted);
  border-radius: var(--radius-lg);
  aspect-ratio: 1;
  margin: 0 0 var(--spacing-4);
}

section[aria-label="Gallery"] ul {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--spacing-2);
  list-style: none;
  padding: 0;
}

section[aria-label="Gallery"] li {
  background: var(--muted);
  border-radius: var(--radius);
  aspect-ratio: 1;
  border: 1px solid var(--border);
}

section[aria-label="Gallery"] li[aria-selected="true"] {
  border: 2px solid var(--primary);
}

/* Details section */
section[aria-label="Details"] mark {
  display: inline-block;
  padding: 2px 8px;
  border-radius: var(--radius);
  font-size: var(--text-xs);
  margin-bottom: var(--spacing-2);
  background: green;
  color: white;
}

section[aria-label="Details"] h1 {
  font-size: var(--text-2xl);
  font-weight: 700;
  margin-bottom: var(--spacing-2);
}

div[aria-label="Rating"] {
  display: flex;
  align-items: center;
  gap: var(--spacing-2);
  margin-bottom: var(--spacing-4);
}

div[aria-label="Rating"] data {
  color: gold;
}

div[aria-label="Rating"] span {
  font-size: var(--text-sm);
  color: var(--muted-foreground);
}

div[aria-label="Price"] {
  margin-bottom: var(--spacing-4);
}

div[aria-label="Price"] data {
  font-size: var(--text-3xl);
  font-weight: 700;
}

div[aria-label="Price"] s {
  font-size: var(--text-lg);
  color: var(--muted-foreground);
  margin-left: var(--spacing-2);
}

section[aria-label="Details"] > p {
  color: var(--muted-foreground);
  margin-bottom: var(--spacing-6);
}

section[aria-label="Details"] fieldset {
  border: none;
  padding: 0;
  margin: 0 0 var(--spacing-4);
}

section[aria-label="Details"] fieldset legend {
  font-weight: 500;
  margin-bottom: var(--spacing-2);
}

fieldset[aria-label="Color"] menu {
  display: flex;
  gap: var(--spacing-2);
  list-style: none;
}

fieldset[aria-label="Color"] button {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  border: 1px solid var(--border);
}

fieldset[aria-label="Color"] button[aria-pressed="true"] {
  border: 2px solid var(--primary);
}

fieldset[aria-label="Quantity"] > div {
  display: flex;
  align-items: center;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  width: fit-content;
}

fieldset[aria-label="Quantity"] button {
  padding: var(--spacing-2) var(--spacing-3);
  background: none;
  border: none;
}

fieldset[aria-label="Quantity"] output {
  padding: 0 var(--spacing-3);
}

section[aria-label="Details"] > menu {
  display: flex;
  gap: var(--spacing-3);
  margin-top: var(--spacing-6);
  list-style: none;
}

/* Primary: first action (Add to Cart) */
section[aria-label="Details"] > menu > :first-child button {
  flex: 1;
  padding: var(--spacing-3);
  background: var(--primary);
  color: var(--primary-foreground);
  border: none;
  border-radius: var(--radius);
  font-weight: 500;
}

/* Secondary: other actions (Wishlist) */
section[aria-label="Details"] > menu > :not(:first-child) button {
  padding: var(--spacing-3);
  background: transparent;
  border: 1px solid var(--border);
  border-radius: var(--radius);
}
```

## When to Use

- E-commerce product pages
- Digital product listings
- Service detail pages
- Item showcases
