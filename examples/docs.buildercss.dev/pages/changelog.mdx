---
$type: DocsPage
title: Changelog Page
description: Product updates and release notes
screenshot: /screenshots/pages/changelog.png
---

# Changelog Page

A changelog page displaying product updates and release history.

<Preview>
  <main>
    <header>
      <h1>Changelog</h1>
      <p>New updates and improvements to Builder CSS</p>
    </header>

    <section>
      <article aria-current="true">
        <header>
          <mark>v2.0.0</mark>
          <time datetime="2024-12-01">December 1, 2024</time>
        </header>
        <h2>Major Release: Builder CSS 2.0</h2>
        <ul>
          <li>Complete rewrite with improved performance</li>
          <li>New semantic HTML-based API</li>
          <li>Dark mode support out of the box</li>
          <li>50+ new components added</li>
        </ul>
      </article>

      <article>
        <header>
          <mark>v1.5.0</mark>
          <time datetime="2024-11-15">November 15, 2024</time>
        </header>
        <h2>New Components & Bug Fixes</h2>
        <ul>
          <li>Added Calendar component</li>
          <li>Added Kanban board component</li>
          <li>Fixed modal focus trap issue</li>
          <li>Improved accessibility across all components</li>
        </ul>
      </article>

      <article>
        <header>
          <mark>v1.4.0</mark>
          <time datetime="2024-10-30">October 30, 2024</time>
        </header>
        <h2>Performance Improvements</h2>
        <ul>
          <li>30% smaller CSS bundle size</li>
          <li>Optimized animations for 60fps</li>
          <li>Reduced JavaScript footprint</li>
        </ul>
      </article>
    </section>
  </main>
</Preview>

## Semantic Pattern

A changelog uses `<main>` with header and section of release articles. The pattern `main:has(> section > article > header > mark + time)` identifies changelog pages:

```html
<main>
  <header>
    <h1>Changelog</h1>
    <p>New updates and improvements to Builder CSS</p>
  </header>

  <section>
    <article aria-current="true">
      <header>
        <mark>v2.0.0</mark>
        <time datetime="2024-12-01">December 1, 2024</time>
      </header>
      <h2>Major Release: Builder CSS 2.0</h2>
      <ul>
        <li>Complete rewrite with improved performance</li>
        <li>New semantic HTML-based API</li>
        <li>Dark mode support out of the box</li>
      </ul>
    </article>

    <article>
      <header>
        <mark>v1.5.0</mark>
        <time datetime="2024-11-15">November 15, 2024</time>
      </header>
      <h2>New Components & Bug Fixes</h2>
      <ul>
        <li>Added Calendar component</li>
        <li>Added Kanban board component</li>
      </ul>
    </article>
  </section>
</main>
```

## CSS

```css
/* Changelog: main with section of release articles */
main:has(> section > article > header > mark + time) {
  padding: var(--spacing-6);
  max-width: 700px;
  margin: 0 auto;
}

main:has(> section > article > header > mark + time) > header {
  margin-bottom: var(--spacing-8);
}

main:has(> section > article > header > mark + time) > header h1 {
  font-size: var(--text-3xl);
  font-weight: 700;
  margin-bottom: var(--spacing-2);
}

main:has(> section > article > header > mark + time) > header p {
  color: var(--muted-foreground);
}

main:has(> section > article > header > mark + time) > section {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-8);
}

/* Timeline styling */
main:has(> section > article > header > mark + time) > section > article {
  padding-left: var(--spacing-6);
  border-left: 2px solid var(--border);
  position: relative;
}

main:has(> section > article > header > mark + time) > section > article::before {
  content: "";
  position: absolute;
  left: -6px;
  top: 0;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--muted);
}

/* Latest release: aria-current */
main:has(> section > article > header > mark + time) > section > article[aria-current]::before {
  background: var(--primary);
}

main:has(> section > article > header > mark + time) > section > article > header {
  display: flex;
  align-items: center;
  gap: var(--spacing-2);
  margin-bottom: var(--spacing-2);
}

main:has(> section > article > header > mark + time) > section > article mark {
  padding: 2px 8px;
  background: var(--muted);
  border-radius: var(--radius);
  font-size: var(--text-xs);
}

main:has(> section > article > header > mark + time) > section > article[aria-current] mark {
  background: var(--primary);
  color: var(--primary-foreground);
}

main:has(> section > article > header > mark + time) > section > article time {
  font-size: var(--text-sm);
  color: var(--muted-foreground);
}

main:has(> section > article > header > mark + time) > section > article h2 {
  font-size: var(--text-lg);
  font-weight: 600;
  margin-bottom: var(--spacing-3);
}

main:has(> section > article > header > mark + time) > section > article ul {
  color: var(--muted-foreground);
  font-size: var(--text-sm);
  padding-left: var(--spacing-4);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-2);
}
```

## When to Use

- Product changelog
- Release notes pages
- Update history
- Version documentation
