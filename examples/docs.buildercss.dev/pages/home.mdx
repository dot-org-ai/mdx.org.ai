---
$type: DocsPage
title: Home Page
description: Marketing landing page for your product
screenshot: /screenshots/pages/home.png
---

# Home Page

A marketing landing page template combining hero, features, testimonials, and CTA sections.

<Preview>
  <article data-page="home">
    <header>
      <a href="/">Acme</a>
      <nav>
        <a href="#features" aria-current="page">Features</a>
        <a href="#pricing">Pricing</a>
        <a href="#docs">Docs</a>
      </nav>
      <button>Get Started</button>
    </header>

    <section aria-label="Hero">
      <mark>New: v2.0 Released</mark>
      <h1>Build better products with semantic HTML</h1>
      <p>A design system that uses data attributes instead of utility classes. Clean HTML, powerful styling.</p>
      <menu>
        <button>Start Building</button>
        <button>View Docs</button>
      </menu>
    </section>

    <section aria-label="Social proof">
      <p>Trusted by teams at</p>
      <ul>
        <li>Vercel</li>
        <li>Stripe</li>
        <li>Linear</li>
        <li>Notion</li>
      </ul>
    </section>
  </article>
</Preview>

## When to Use

- Product marketing landing pages
- SaaS homepage
- Startup websites
- Product launches

## Structure

```html
<article data-page="home">
  <header>
    <a href="/">Acme</a>
    <nav>
      <a href="#features">Features</a>
      <a href="#pricing">Pricing</a>
      <a href="#docs">Docs</a>
    </nav>
    <button>Get Started</button>
  </header>

  <section aria-label="Hero">
    <mark>New: v2.0 Released</mark>
    <h1>Build better products with semantic HTML</h1>
    <p>A design system that uses data attributes...</p>
    <menu>
      <button>Start Building</button>
      <button>View Docs</button>
    </menu>
  </section>

  <section aria-label="Social proof">
    <p>Trusted by teams at</p>
    <ul>
      <li><img src="/logos/vercel.svg" alt="Vercel" /></li>
      <li><img src="/logos/stripe.svg" alt="Stripe" /></li>
    </ul>
  </section>

  <section aria-label="Features">
    <!-- Feature cards -->
  </section>

  <section aria-label="Testimonials">
    <!-- Testimonial cards -->
  </section>

  <section aria-label="Call to action">
    <h2>Ready to get started?</h2>
    <button>Start Free Trial</button>
  </section>

  <footer>
    <!-- Footer content -->
  </footer>
</article>
```

## CSS

```css
[data-page="home"] {
  min-height: 100vh;
}

[data-page="home"] > header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-4) var(--spacing-6);
  border-bottom: 1px solid var(--border);
  position: sticky;
  top: 0;
  background: var(--background);
  z-index: 50;
}

[data-page="home"] > header > a:first-child {
  font-weight: 700;
  font-size: var(--text-xl);
}

[data-page="home"] > header nav {
  display: flex;
  gap: var(--spacing-6);
  font-size: var(--text-sm);
}

[data-page="home"] > header nav a {
  color: var(--muted-foreground);
}

[data-page="home"] > header nav a[aria-current="page"] {
  color: var(--foreground);
}

[data-page="home"] > header > button {
  padding: var(--spacing-2) var(--spacing-4);
  background: var(--primary);
  color: var(--primary-foreground);
  border: none;
  border-radius: var(--radius);
}

[data-page="home"] > section[aria-label="Hero"] {
  padding: var(--spacing-16) var(--spacing-6);
  text-align: center;
  max-width: 800px;
  margin: 0 auto;
}

[data-page="home"] > section[aria-label="Hero"] mark {
  display: inline-block;
  padding: var(--spacing-1) var(--spacing-3);
  background: var(--muted);
  border-radius: var(--radius-full);
  font-size: var(--text-sm);
  margin-bottom: var(--spacing-4);
}

[data-page="home"] > section[aria-label="Hero"] h1 {
  font-size: var(--text-4xl);
  font-weight: 800;
  line-height: 1.1;
  margin-bottom: var(--spacing-4);
}

[data-page="home"] > section[aria-label="Hero"] p {
  font-size: var(--text-lg);
  color: var(--muted-foreground);
  margin-bottom: var(--spacing-6);
}

[data-page="home"] > section[aria-label="Hero"] menu {
  display: flex;
  gap: var(--spacing-3);
  justify-content: center;
}

[data-page="home"] > section[aria-label="Hero"] menu button:first-child {
  padding: var(--spacing-3) var(--spacing-6);
  background: var(--primary);
  color: var(--primary-foreground);
  border: none;
  border-radius: var(--radius);
  font-weight: 500;
}

[data-page="home"] > section[aria-label="Hero"] menu button:last-child {
  padding: var(--spacing-3) var(--spacing-6);
  background: transparent;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-weight: 500;
}

[data-page="home"] > section[aria-label="Social proof"] {
  padding: var(--spacing-8) var(--spacing-6);
  background: var(--muted);
}

[data-page="home"] > section[aria-label="Social proof"] p {
  text-align: center;
  font-size: var(--text-sm);
  color: var(--muted-foreground);
  margin-bottom: var(--spacing-4);
}

[data-page="home"] > section[aria-label="Social proof"] ul {
  display: flex;
  justify-content: center;
  gap: var(--spacing-8);
  list-style: none;
  padding: 0;
  opacity: 0.5;
}

[data-page="home"] > section[aria-label="Social proof"] li {
  font-weight: 600;
}
```
