---
$type: DocsPage
title: Pricing Page
description: Subscription plans and pricing tiers
screenshot: /screenshots/pages/pricing.png
---

# Pricing Page

A pricing page with comparison tiers and feature lists.

<Preview>
  <article data-page="pricing" style={{ padding: 'var(--spacing-6)' }}>
    <header style={{ textAlign: 'center', maxWidth: '600px', margin: '0 auto var(--spacing-8)' }}>
      <h1 style={{ fontSize: 'var(--text-3xl)', fontWeight: 700, marginBottom: 'var(--spacing-3)' }}>Simple, transparent pricing</h1>
      <p style={{ color: 'var(--muted-foreground)' }}>Choose the plan that's right for you</p>
      <div style={{ display: 'flex', justifyContent: 'center', gap: 'var(--spacing-2)', marginTop: 'var(--spacing-4)', background: 'var(--muted)', padding: 'var(--spacing-1)', borderRadius: 'var(--radius)', width: 'fit-content', margin: 'var(--spacing-4) auto 0' }}>
        <button style={{ padding: 'var(--spacing-2) var(--spacing-4)', background: 'var(--background)', border: 'none', borderRadius: 'var(--radius)', fontWeight: 500 }}>Monthly</button>
        <button style={{ padding: 'var(--spacing-2) var(--spacing-4)', background: 'transparent', border: 'none', borderRadius: 'var(--radius)', color: 'var(--muted-foreground)' }}>Annual (Save 20%)</button>
      </div>
    </header>

    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 'var(--spacing-4)', maxWidth: '1000px', margin: '0 auto' }}>
      <div style={{ border: '1px solid var(--border)', borderRadius: 'var(--radius-lg)', padding: 'var(--spacing-6)' }}>
        <h3 style={{ fontWeight: 600, marginBottom: 'var(--spacing-2)' }}>Starter</h3>
        <p style={{ fontSize: 'var(--text-sm)', color: 'var(--muted-foreground)', marginBottom: 'var(--spacing-4)' }}>For individuals and small projects</p>
        <div style={{ marginBottom: 'var(--spacing-4)' }}>
          <span style={{ fontSize: 'var(--text-3xl)', fontWeight: 700 }}>$0</span>
          <span style={{ color: 'var(--muted-foreground)' }}>/month</span>
        </div>
        <button style={{ width: '100%', padding: 'var(--spacing-2)', background: 'transparent', border: '1px solid var(--border)', borderRadius: 'var(--radius)', marginBottom: 'var(--spacing-4)' }}>Get Started</button>
        <ul style={{ fontSize: 'var(--text-sm)', listStyle: 'none', padding: 0 }}>
          {['Up to 3 projects', '1GB storage', 'Community support'].map(feature => (
            <li key={feature} style={{ padding: 'var(--spacing-1) 0', display: 'flex', alignItems: 'center', gap: 'var(--spacing-2)' }}>✓ {feature}</li>
          ))}
        </ul>
      </div>

      <div style={{ border: '2px solid var(--primary)', borderRadius: 'var(--radius-lg)', padding: 'var(--spacing-6)', position: 'relative' }}>
        <span style={{ position: 'absolute', top: '-12px', left: '50%', transform: 'translateX(-50%)', background: 'var(--primary)', color: 'var(--primary-foreground)', padding: '2px 12px', borderRadius: 'var(--radius-full)', fontSize: 'var(--text-xs)' }}>Popular</span>
        <h3 style={{ fontWeight: 600, marginBottom: 'var(--spacing-2)' }}>Pro</h3>
        <p style={{ fontSize: 'var(--text-sm)', color: 'var(--muted-foreground)', marginBottom: 'var(--spacing-4)' }}>For growing teams</p>
        <div style={{ marginBottom: 'var(--spacing-4)' }}>
          <span style={{ fontSize: 'var(--text-3xl)', fontWeight: 700 }}>$29</span>
          <span style={{ color: 'var(--muted-foreground)' }}>/month</span>
        </div>
        <button style={{ width: '100%', padding: 'var(--spacing-2)', background: 'var(--primary)', color: 'var(--primary-foreground)', border: 'none', borderRadius: 'var(--radius)', marginBottom: 'var(--spacing-4)' }}>Start Free Trial</button>
        <ul style={{ fontSize: 'var(--text-sm)', listStyle: 'none', padding: 0 }}>
          {['Unlimited projects', '100GB storage', 'Priority support', 'Advanced analytics'].map(feature => (
            <li key={feature} style={{ padding: 'var(--spacing-1) 0', display: 'flex', alignItems: 'center', gap: 'var(--spacing-2)' }}>✓ {feature}</li>
          ))}
        </ul>
      </div>

      <div style={{ border: '1px solid var(--border)', borderRadius: 'var(--radius-lg)', padding: 'var(--spacing-6)' }}>
        <h3 style={{ fontWeight: 600, marginBottom: 'var(--spacing-2)' }}>Enterprise</h3>
        <p style={{ fontSize: 'var(--text-sm)', color: 'var(--muted-foreground)', marginBottom: 'var(--spacing-4)' }}>For large organizations</p>
        <div style={{ marginBottom: 'var(--spacing-4)' }}>
          <span style={{ fontSize: 'var(--text-3xl)', fontWeight: 700 }}>Custom</span>
        </div>
        <button style={{ width: '100%', padding: 'var(--spacing-2)', background: 'transparent', border: '1px solid var(--border)', borderRadius: 'var(--radius)', marginBottom: 'var(--spacing-4)' }}>Contact Sales</button>
        <ul style={{ fontSize: 'var(--text-sm)', listStyle: 'none', padding: 0 }}>
          {['Everything in Pro', 'Unlimited storage', 'Dedicated support', 'Custom integrations', 'SLA guarantee'].map(feature => (
            <li key={feature} style={{ padding: 'var(--spacing-1) 0', display: 'flex', alignItems: 'center', gap: 'var(--spacing-2)' }}>✓ {feature}</li>
          ))}
        </ul>
      </div>
    </div>
  </article>
</Preview>

## When to Use

- SaaS pricing pages
- Subscription plan comparisons
- Membership tiers
- Service packages

## Structure

```html
<article data-page="pricing">
  <header data-page-header>
    <h1>Simple, transparent pricing</h1>
    <p>Choose the plan that's right for you</p>
    <div data-billing-toggle>
      <button data-active>Monthly</button>
      <button>Annual (Save 20%)</button>
    </div>
  </header>

  <section data-pricing-grid>
    <article data-pricing-card>
      <h3>Starter</h3>
      <p>For individuals and small projects</p>
      <div data-price>
        <span data-amount>$0</span>
        <span data-period>/month</span>
      </div>
      <button data-button="outline">Get Started</button>
      <ul data-features>
        <li>Up to 3 projects</li>
        <li>1GB storage</li>
      </ul>
    </article>

    <article data-pricing-card data-featured>
      <span data-badge>Popular</span>
      <h3>Pro</h3>
      <!-- ... -->
      <button data-button="primary">Start Free Trial</button>
    </article>

    <article data-pricing-card>
      <h3>Enterprise</h3>
      <!-- ... -->
      <button data-button="outline">Contact Sales</button>
    </article>
  </section>
</article>
```

## CSS

```css
[data-page="pricing"] {
  padding: var(--spacing-6);
}

[data-page="pricing"] [data-page-header] {
  text-align: center;
  max-width: 600px;
  margin: 0 auto var(--spacing-8);
}

[data-page="pricing"] [data-billing-toggle] {
  display: flex;
  gap: var(--spacing-2);
  background: var(--muted);
  padding: var(--spacing-1);
  border-radius: var(--radius);
  width: fit-content;
  margin: var(--spacing-4) auto 0;
}

[data-page="pricing"] [data-pricing-grid] {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spacing-4);
  max-width: 1000px;
  margin: 0 auto;
}

[data-page="pricing"] [data-pricing-card] {
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-6);
}

[data-page="pricing"] [data-pricing-card][data-featured] {
  border: 2px solid var(--primary);
  position: relative;
}

[data-page="pricing"] [data-pricing-card][data-featured] [data-badge] {
  position: absolute;
  top: -12px;
  left: 50%;
  transform: translateX(-50%);
}

[data-page="pricing"] [data-price] [data-amount] {
  font-size: var(--text-3xl);
  font-weight: 700;
}

[data-page="pricing"] [data-features] {
  list-style: none;
  padding: 0;
  font-size: var(--text-sm);
}

[data-page="pricing"] [data-features] li::before {
  content: "✓ ";
  color: var(--primary);
}
```
