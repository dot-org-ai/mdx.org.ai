---
$type: DocsPage
title: Waitlist Page
description: Early access and waitlist signup
screenshot: /screenshots/pages/waitlist.png
---

# Waitlist Page

A waitlist page for early access signups and launches.

<Preview>
  <article data-page="waitlist">
    <mark>Coming Soon</mark>
    <h1>Be the first to experience Builder CSS 3.0</h1>
    <p>Join the waitlist to get early access, exclusive updates, and founder pricing.</p>

    <form>
      <input type="email" placeholder="Enter your email" required />
      <button type="submit">Join Waitlist</button>
    </form>

    <p aria-label="Count">
      <data value="2847">2,847</data> people already joined
    </p>

    <section aria-label="Perks">
      <article>
        <span aria-hidden="true">🚀</span>
        <h3>Early Access</h3>
        <p>Get access before public launch</p>
      </article>
      <article>
        <span aria-hidden="true">💎</span>
        <h3>Founder Pricing</h3>
        <p>Lock in 50% lifetime discount</p>
      </article>
      <article>
        <span aria-hidden="true">🎁</span>
        <h3>Exclusive Content</h3>
        <p>Design resources and templates</p>
      </article>
    </section>
  </article>
</Preview>

## When to Use

- Product launch waitlists
- Early access signups
- Beta program registration
- Coming soon pages

## Structure

```html
<article data-page="waitlist">
  <mark>Coming Soon</mark>
  <h1>Be the first to experience Builder CSS 3.0</h1>
  <p>Join the waitlist to get early access...</p>

  <form>
    <input type="email" placeholder="Enter your email" required />
    <button type="submit">Join Waitlist</button>
  </form>

  <p aria-label="Count">
    <data value="2847">2,847</data> people already joined
  </p>

  <section aria-label="Perks">
    <article>
      <span aria-hidden="true">🚀</span>
      <h3>Early Access</h3>
      <p>Get access before public launch</p>
    </article>
    <article>
      <span aria-hidden="true">💎</span>
      <h3>Founder Pricing</h3>
      <p>Lock in 50% lifetime discount</p>
    </article>
    <article>
      <span aria-hidden="true">🎁</span>
      <h3>Exclusive Content</h3>
      <p>Design resources and templates</p>
    </article>
  </section>
</article>
```

## CSS

```css
[data-page="waitlist"] {
  padding: var(--spacing-6);
  text-align: center;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(to bottom, var(--background), var(--muted));
}

[data-page="waitlist"] > mark {
  padding: var(--spacing-2) var(--spacing-4);
  background: var(--primary);
  color: var(--primary-foreground);
  border-radius: var(--radius-full);
  font-size: var(--text-sm);
  margin-bottom: var(--spacing-4);
}

[data-page="waitlist"] h1 {
  font-size: var(--text-4xl);
  font-weight: 800;
  margin-bottom: var(--spacing-3);
  max-width: 600px;
}

[data-page="waitlist"] > p {
  font-size: var(--text-lg);
  color: var(--muted-foreground);
  margin-bottom: var(--spacing-6);
  max-width: 500px;
}

[data-page="waitlist"] form {
  display: flex;
  gap: var(--spacing-2);
  max-width: 400px;
  width: 100%;
  margin-bottom: var(--spacing-4);
}

[data-page="waitlist"] form input {
  flex: 1;
  padding: var(--spacing-3);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-size: var(--text-sm);
}

[data-page="waitlist"] form button {
  padding: var(--spacing-3) var(--spacing-6);
  background: var(--primary);
  color: var(--primary-foreground);
  border: none;
  border-radius: var(--radius);
  font-weight: 500;
  white-space: nowrap;
}

[data-page="waitlist"] p[aria-label="Count"] {
  font-size: var(--text-sm);
  color: var(--muted-foreground);
  margin-bottom: var(--spacing-8);
}

[data-page="waitlist"] p[aria-label="Count"] data {
  font-weight: 600;
  color: var(--foreground);
}

[data-page="waitlist"] section[aria-label="Perks"] {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spacing-6);
  max-width: 600px;
}

[data-page="waitlist"] section[aria-label="Perks"] article {
  text-align: center;
}

[data-page="waitlist"] section[aria-label="Perks"] span {
  font-size: var(--text-2xl);
  display: block;
  margin-bottom: var(--spacing-2);
}

[data-page="waitlist"] section[aria-label="Perks"] h3 {
  font-weight: 600;
  font-size: var(--text-sm);
  margin-bottom: var(--spacing-1);
}

[data-page="waitlist"] section[aria-label="Perks"] p {
  font-size: var(--text-xs);
  color: var(--muted-foreground);
}
```

## JavaScript

```js
const form = document.querySelector('[data-page="waitlist"] form')
const countEl = document.querySelector('[data-page="waitlist"] data')

form.addEventListener('submit', async (e) => {
  e.preventDefault()
  const email = form.querySelector('input').value

  // Submit to API
  const res = await fetch('/api/waitlist', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email })
  })

  if (res.ok) {
    // Update count and show success
    const { count } = await res.json()
    countEl.textContent = count.toLocaleString()
    form.innerHTML = '<p>🎉 You\'re on the list!</p>'
  }
})
```
