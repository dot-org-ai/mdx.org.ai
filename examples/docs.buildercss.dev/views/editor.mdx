---
$type: DocsPage
title: Editor View
description: Code/text editor (Monaco-based)
screenshot: /screenshots/views/editor.png
---

# Editor View

A code or text editor view, typically Monaco-based for rich editing.

<Preview>
  <article data-view="editor">
    <header>
      <div>
        <span>📄 index.ts</span>
        <mark>Modified</mark>
      </div>
      <menu>
        <button>Format</button>
        <button>Save</button>
      </menu>
    </header>
    <div>
      <aside>
        <span>1</span>
        <span>2</span>
        <span>3</span>
        <span>4</span>
        <span>5</span>
        <span>6</span>
        <span>7</span>
        <span>8</span>
      </aside>
      <pre><code>{`import { Database } from 'mdxdb'

export async function handler(req) {
  const db = new Database()
  const items = await db.list()

  return Response.json(items)
}`}</code></pre>
    </div>
    <footer>
      <span>TypeScript</span>
      <span>Ln 3, Col 12</span>
    </footer>
  </article>
</Preview>

## When to Use

- Code editors
- Text editors
- Configuration editing
- Documentation editing
- JSON/YAML editing

## Structure

```html
<article data-view="editor">
  <header>
    <div>
      <span>📄 index.ts</span>
      <mark>Modified</mark>
    </div>
    <menu>
      <button>Format</button>
      <button>Save</button>
    </menu>
  </header>

  <div>
    <aside>
      <span>1</span>
      <span>2</span>
      <!-- Line numbers -->
    </aside>
    <pre><code><!-- Editor content --></code></pre>
  </div>

  <footer>
    <span>TypeScript</span>
    <span>Ln 3, Col 12</span>
  </footer>
</article>
```

## CSS

```css
[data-view="editor"] {
  display: flex;
  flex-direction: column;
  height: 100%;
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

[data-view="editor"] > header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-2) var(--spacing-4);
  background: var(--muted);
  border-bottom: 1px solid var(--border);
}

[data-view="editor"] > header > div {
  display: flex;
  align-items: center;
  gap: var(--spacing-2);
}

[data-view="editor"] > header > div span {
  font-size: var(--text-sm);
}

[data-view="editor"] > header mark {
  font-size: var(--text-xs);
  background: transparent;
  color: var(--muted-foreground);
}

[data-view="editor"] > header menu {
  display: flex;
  gap: var(--spacing-2);
}

[data-view="editor"] > header menu button:first-child {
  padding: var(--spacing-1) var(--spacing-3);
  background: transparent;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-size: var(--text-sm);
}

[data-view="editor"] > header menu button:last-child {
  padding: var(--spacing-1) var(--spacing-3);
  background: var(--primary);
  color: var(--primary-foreground);
  border: none;
  border-radius: var(--radius);
  font-size: var(--text-sm);
}

[data-view="editor"] > div {
  display: flex;
  flex: 1;
  overflow: hidden;
}

[data-view="editor"] > div > aside {
  width: 40px;
  background: var(--muted);
  border-right: 1px solid var(--border);
  padding: var(--spacing-2);
  font-family: monospace;
  font-size: var(--text-xs);
  color: var(--muted-foreground);
  text-align: right;
  display: flex;
  flex-direction: column;
}

[data-view="editor"] > div > pre {
  flex: 1;
  padding: var(--spacing-2);
  margin: 0;
  font-family: monospace;
  font-size: var(--text-sm);
  line-height: 1.5;
  overflow: auto;
}

[data-view="editor"] > footer {
  display: flex;
  justify-content: space-between;
  padding: var(--spacing-1) var(--spacing-4);
  background: var(--muted);
  border-top: 1px solid var(--border);
  font-size: var(--text-xs);
  color: var(--muted-foreground);
}
```

## Integration with Monaco

```tsx
import * as monaco from 'monaco-editor'

function Editor({ value, language, onChange }) {
  const containerRef = useRef(null)
  const editorRef = useRef(null)

  useEffect(() => {
    editorRef.current = monaco.editor.create(containerRef.current, {
      value,
      language,
      theme: 'vs-dark',
      minimap: { enabled: false },
      automaticLayout: true,
    })

    editorRef.current.onDidChangeModelContent(() => {
      onChange?.(editorRef.current.getValue())
    })

    return () => editorRef.current?.dispose()
  }, [])

  return <div ref={containerRef} />
}
```
