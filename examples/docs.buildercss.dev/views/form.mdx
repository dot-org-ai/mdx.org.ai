---
$type: DocsPage
title: Form View
description: Create/edit form with validation
screenshot: /screenshots/views/form.png
---

# Form View

A form for creating or editing data with validation.

<Preview>
  <article>
    <header>
      <h1>Create Project</h1>
    </header>
    <form>
      <div>
        <label>Project Name *</label>
        <input type="text" placeholder="Enter project name" required />
      </div>
      <div>
        <label>Description</label>
        <textarea rows="4" placeholder="Describe your project..."></textarea>
      </div>
      <fieldset>
        <div>
          <label>Category</label>
          <select>
            <option>Select category</option>
            <option>Development</option>
            <option>Design</option>
            <option>Marketing</option>
          </select>
        </div>
        <div>
          <label>Priority</label>
          <select>
            <option>Low</option>
            <option>Medium</option>
            <option>High</option>
          </select>
        </div>
      </fieldset>
      <div>
        <label>
          <input type="checkbox" />
          <span>Make this project public</span>
        </label>
      </div>
    </form>
    <footer>
      <menu>
        <li><button type="button">Cancel</button></li>
        <li><button type="submit">Create Project</button></li>
      </menu>
    </footer>
  </article>
</Preview>

## Semantic Pattern

A form view is an `<article>` containing a `<form>` element:

```html
<article>
  <header>
    <h1>Create Project</h1>
  </header>

  <form>
    <div>
      <label for="name">Project Name *</label>
      <input type="text" id="name" required />
      <span role="alert">This field is required</span>
    </div>

    <div>
      <label for="description">Description</label>
      <textarea id="description" rows="4"></textarea>
      <small>Optional description of your project</small>
    </div>

    <fieldset>
      <div>
        <label for="category">Category</label>
        <select id="category">
          <option value="">Select category</option>
          <option value="dev">Development</option>
          <option value="design">Design</option>
        </select>
      </div>
      <div>
        <label for="priority">Priority</label>
        <select id="priority">
          <option value="low">Low</option>
          <option value="medium">Medium</option>
          <option value="high">High</option>
        </select>
      </div>
    </fieldset>

    <div>
      <label>
        <input type="checkbox" name="public" />
        Make this project public
      </label>
    </div>
  </form>

  <footer>
    <menu>
      <li><button type="button">Cancel</button></li>
      <li><button type="submit">Create Project</button></li>
    </menu>
  </footer>
</article>
```

## CSS

```css
/* Form view: article containing form */
article:has(> form) {
  padding: var(--spacing-6);
  max-width: 600px;
}

article:has(> form) > header {
  margin-bottom: var(--spacing-6);
}

article:has(> form) > header h1 {
  font-size: var(--text-xl);
  font-weight: 700;
}

article:has(> form) form {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4);
}

article:has(> form) form > div {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-1);
}

article:has(> form) form label {
  font-weight: 500;
  font-size: var(--text-sm);
}

article:has(> form) form input[type="text"],
article:has(> form) form input[type="email"],
article:has(> form) form input[type="password"],
article:has(> form) form textarea,
article:has(> form) form select {
  width: 100%;
  padding: var(--spacing-2);
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

article:has(> form) form textarea {
  resize: vertical;
}

article:has(> form) form input:focus,
article:has(> form) form textarea:focus,
article:has(> form) form select:focus {
  outline: 2px solid var(--ring);
  outline-offset: 2px;
}

article:has(> form) form fieldset {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--spacing-4);
  border: none;
  padding: 0;
  margin: 0;
}

article:has(> form) form [role="alert"] {
  color: var(--destructive);
  font-size: var(--text-sm);
  display: none;
}

article:has(> form) form div:has(:invalid:not(:placeholder-shown)) [role="alert"] {
  display: block;
}

article:has(> form) form small {
  color: var(--muted-foreground);
  font-size: var(--text-sm);
}

article:has(> form) form label:has(input[type="checkbox"]) {
  display: flex;
  align-items: center;
  gap: var(--spacing-2);
  cursor: pointer;
}

article:has(> form) > footer {
  margin-top: var(--spacing-4);
  padding-top: var(--spacing-4);
  border-top: 1px solid var(--border);
}

article:has(> form) > footer menu {
  display: flex;
  gap: var(--spacing-2);
  justify-content: flex-end;
  list-style: none;
}

/* Secondary button: first in menu */
article:has(> form) > footer menu > :first-child button {
  padding: var(--spacing-2) var(--spacing-4);
  background: transparent;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  cursor: pointer;
}

/* Primary button: last in menu */
article:has(> form) > footer menu > :last-child button {
  padding: var(--spacing-2) var(--spacing-4);
  background: var(--primary);
  color: var(--primary-foreground);
  border: none;
  border-radius: var(--radius);
  cursor: pointer;
}
```

## When to Use

- Create/edit forms
- Settings forms
- Multi-step wizards
- Data entry screens
