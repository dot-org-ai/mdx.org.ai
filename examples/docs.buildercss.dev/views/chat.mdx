---
$type: DocsPage
title: Chat View
description: Chat/messaging interface
screenshot: /screenshots/views/chat.png
---

# Chat View

A chat/messaging interface for real-time communication.

<Preview>
  <article>
    <header>
      <figure></figure>
      <div>
        <h2>Support Team</h2>
        <span>Online</span>
      </div>
    </header>
    <section>
      <div>
        <p>Hi! How can I help you today?</p>
        <time>10:30 AM</time>
      </div>
      <div aria-current="true">
        <p>I have a question about my order #12345</p>
        <time>10:31 AM</time>
      </div>
      <div>
        <p>Of course! Let me look that up for you. One moment please...</p>
        <time>10:32 AM</time>
      </div>
    </section>
    <footer>
      <button aria-label="Attach file">📎</button>
      <input type="text" placeholder="Type a message..." />
      <button>Send</button>
    </footer>
  </article>
</Preview>

## Semantic Pattern

A chat view uses `aria-current="true"` to mark the current user's messages:

```html
<article>
  <header>
    <figure><img src="/avatar.jpg" alt="" /></figure>
    <div>
      <h2>Support Team</h2>
      <span>Online</span>
    </div>
  </header>

  <section>
    <!-- Other person's message -->
    <div>
      <p>Hi! How can I help you today?</p>
      <time>10:30 AM</time>
    </div>

    <!-- Current user's message (aria-current) -->
    <div aria-current="true">
      <p>I have a question about my order</p>
      <time>10:31 AM</time>
    </div>
  </section>

  <footer>
    <button aria-label="Attach file">📎</button>
    <input type="text" placeholder="Type a message..." />
    <button>Send</button>
  </footer>
</article>
```

## CSS

```css
/* Chat view: article with header + section (messages) + footer (input) */
article:has(> header + section + footer > input) {
  display: flex;
  flex-direction: column;
  height: 100%;
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

article:has(> header + section + footer > input) > header {
  display: flex;
  align-items: center;
  gap: var(--spacing-3);
  padding: var(--spacing-4);
  border-bottom: 1px solid var(--border);
}

article:has(> header + section + footer > input) > header figure {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--primary);
  margin: 0;
}

article:has(> header + section + footer > input) > header h2 {
  font-weight: 600;
}

article:has(> header + section + footer > input) > header span {
  font-size: var(--text-sm);
  color: green;
}

article:has(> header + section + footer > input) > header span::before {
  content: '● ';
}

article:has(> header + section + footer > input) > section {
  flex: 1;
  padding: var(--spacing-4);
  overflow-y: auto;
  background: var(--muted);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-3);
}

article:has(> header + section + footer > input) > section > div {
  max-width: 70%;
  align-self: flex-start;
}

/* Current user's messages use aria-current */
article:has(> header + section + footer > input) > section > div[aria-current="true"] {
  align-self: flex-end;
}

article:has(> header + section + footer > input) > section > div > p {
  padding: var(--spacing-3);
  border-radius: var(--radius-lg);
  background: var(--background);
  border-bottom-left-radius: 0;
}

article:has(> header + section + footer > input) > section > div[aria-current="true"] > p {
  background: var(--primary);
  color: var(--primary-foreground);
  border-bottom-left-radius: var(--radius-lg);
  border-bottom-right-radius: 0;
}

article:has(> header + section + footer > input) > section time {
  font-size: var(--text-xs);
  color: var(--muted-foreground);
  margin-top: 4px;
  display: block;
}

article:has(> header + section + footer > input) > section > div[aria-current="true"] time {
  text-align: right;
}

article:has(> header + section + footer > input) > footer {
  padding: var(--spacing-4);
  border-top: 1px solid var(--border);
  display: flex;
  gap: var(--spacing-2);
}

article:has(> header + section + footer > input) > footer button:first-child {
  padding: var(--spacing-2);
  background: transparent;
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

article:has(> header + section + footer > input) > footer input {
  flex: 1;
  padding: var(--spacing-2);
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

article:has(> header + section + footer > input) > footer button:last-child {
  padding: var(--spacing-2) var(--spacing-4);
  background: var(--primary);
  color: var(--primary-foreground);
  border: none;
  border-radius: var(--radius);
}
```

## JavaScript

```js
const chat = document.querySelector('article:has(> header + section + footer > input)')
const section = chat.querySelector('section')
const input = chat.querySelector('footer input')
const sendBtn = chat.querySelector('footer button:last-child')

function addMessage(text, isSelf = false) {
  const div = document.createElement('div')
  if (isSelf) div.setAttribute('aria-current', 'true')

  const p = document.createElement('p')
  p.textContent = text

  const time = document.createElement('time')
  time.textContent = new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })

  div.appendChild(p)
  div.appendChild(time)
  section.appendChild(div)
  section.scrollTop = section.scrollHeight
}

sendBtn.addEventListener('click', () => {
  if (input.value.trim()) {
    addMessage(input.value, true)
    input.value = ''
  }
})

input.addEventListener('keypress', (e) => {
  if (e.key === 'Enter') sendBtn.click()
})
```

## When to Use

- Support chat
- Direct messaging
- Team chat
- Customer service
- AI assistants
