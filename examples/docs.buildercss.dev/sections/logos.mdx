---
$type: DocsPage
title: Logos Section
description: Logo cloud of trusted companies
screenshot: /screenshots/sections/logos.png
---

# Logos Section

A logo cloud showing trusted companies and partners.

<Preview>
  <section data-section="logos">
    <p>Trusted by teams at</p>
    <div>
      <figure>Company 1</figure>
      <figure>Company 2</figure>
      <figure>Company 3</figure>
      <figure>Company 4</figure>
      <figure>Company 5</figure>
    </div>
  </section>
</Preview>

## When to Use

- Social proof after hero
- Partner showcases
- Client lists
- Integration displays

## Structure

```html
<section data-section="logos">
  <p>Trusted by teams at</p>
  <div>
    <img src="/logos/company1.svg" alt="Company 1" />
    <img src="/logos/company2.svg" alt="Company 2" />
    <img src="/logos/company3.svg" alt="Company 3" />
    <img src="/logos/company4.svg" alt="Company 4" />
    <img src="/logos/company5.svg" alt="Company 5" />
  </div>
</section>
```

## CSS

```css
[data-section="logos"] {
  padding: var(--spacing-12) var(--spacing-4);
  text-align: center;
}

[data-section="logos"] > p {
  color: var(--muted-foreground);
  font-size: var(--text-sm);
  margin-bottom: var(--spacing-8);
}

[data-section="logos"] > div {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: var(--spacing-12);
  flex-wrap: wrap;
}

[data-section="logos"] > div img {
  height: 32px;
  width: auto;
  opacity: 0.6;
  filter: grayscale(100%);
  transition: opacity 0.2s, filter 0.2s;
}

[data-section="logos"] > div img:hover {
  opacity: 1;
  filter: grayscale(0%);
}

[data-section="logos"] > div figure {
  width: 120px;
  height: 40px;
  background: var(--muted);
  border-radius: var(--radius);
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0.6;
}

@media (max-width: 768px) {
  [data-section="logos"] > div {
    gap: var(--spacing-6);
  }

  [data-section="logos"] > div img {
    height: 24px;
  }
}
```

## Variations

### Scrolling Marquee

```html
<section data-section="logos">
  <p>Trusted by teams at</p>
  <div>
    <div>
      <img src="/logos/company1.svg" alt="" />
      <img src="/logos/company2.svg" alt="" />
      <!-- Duplicate for seamless loop -->
    </div>
  </div>
</section>
```

```css
[data-section="logos"] > div {
  overflow: hidden;
  mask-image: linear-gradient(
    to right,
    transparent,
    black 20%,
    black 80%,
    transparent
  );
}

[data-section="logos"] > div > div {
  display: flex;
  gap: var(--spacing-12);
  animation: marquee 30s linear infinite;
}

@keyframes marquee {
  from { transform: translateX(0); }
  to { transform: translateX(-50%); }
}
```

### With Borders

```html
<section data-section="logos">
  <div>
    <a href="https://company1.com">
      <img src="/logos/company1.svg" alt="Company 1" />
    </a>
    <!-- More logos -->
  </div>
</section>
```
