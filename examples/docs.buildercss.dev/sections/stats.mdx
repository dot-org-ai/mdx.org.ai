---
$type: DocsPage
title: Stats Section
description: Key metrics and numbers
screenshot: /screenshots/sections/stats.png
---

# Stats Section

Display key metrics and impressive numbers.

<Preview>
  <section data-section="stats" style={{ padding: 'var(--spacing-16) var(--spacing-4)' }}>
    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 'var(--spacing-8)', textAlign: 'center', maxWidth: '900px', margin: '0 auto' }}>
      <div>
        <div style={{ fontSize: 'var(--text-4xl)', fontWeight: 700, color: 'var(--primary)', marginBottom: 'var(--spacing-2)' }}>10K+</div>
        <div style={{ color: 'var(--muted-foreground)' }}>Active Users</div>
      </div>
      <div>
        <div style={{ fontSize: 'var(--text-4xl)', fontWeight: 700, color: 'var(--primary)', marginBottom: 'var(--spacing-2)' }}>99.9%</div>
        <div style={{ color: 'var(--muted-foreground)' }}>Uptime</div>
      </div>
      <div>
        <div style={{ fontSize: 'var(--text-4xl)', fontWeight: 700, color: 'var(--primary)', marginBottom: 'var(--spacing-2)' }}>50M+</div>
        <div style={{ color: 'var(--muted-foreground)' }}>API Requests</div>
      </div>
      <div>
        <div style={{ fontSize: 'var(--text-4xl)', fontWeight: 700, color: 'var(--primary)', marginBottom: 'var(--spacing-2)' }}>24/7</div>
        <div style={{ color: 'var(--muted-foreground)' }}>Support</div>
      </div>
    </div>
  </section>
</Preview>

## When to Use

- Impact showcases
- Product statistics
- Company milestones
- Performance metrics

## Structure

```html
<section data-section="stats">
  <div data-stats-grid>
    <article data-stat>
      <data value="10000">10K+</data>
      <p>Active Users</p>
    </article>

    <article data-stat>
      <data value="99.9">99.9%</data>
      <p>Uptime</p>
    </article>

    <article data-stat>
      <data value="50000000">50M+</data>
      <p>API Requests</p>
    </article>

    <article data-stat>
      <data value="24">24/7</data>
      <p>Support</p>
    </article>
  </div>
</section>
```

## CSS

```css
[data-section="stats"] {
  padding: var(--spacing-16) var(--spacing-4);
}

[data-section="stats"] [data-stats-grid] {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--spacing-8);
  text-align: center;
  max-width: 900px;
  margin: 0 auto;
}

[data-section="stats"] [data-stat] data {
  display: block;
  font-size: var(--text-4xl);
  font-weight: 700;
  color: var(--primary);
  margin-bottom: var(--spacing-2);
}

[data-section="stats"] [data-stat] p {
  color: var(--muted-foreground);
}

@media (max-width: 768px) {
  [data-section="stats"] [data-stats-grid] {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 480px) {
  [data-section="stats"] [data-stats-grid] {
    grid-template-columns: 1fr;
  }
}
```

## Variations

### With Icons

```html
<section data-section="stats" data-variant="icons">
  <article data-stat>
    <div data-icon>
      <svg><!-- Users icon --></svg>
    </div>
    <data value="10000">10K+</data>
    <p>Active Users</p>
  </article>
</section>
```

### Animated Counters

```html
<section data-section="stats" data-animate>
  <article data-stat>
    <data value="10000" data-counter>0</data>
    <p>Active Users</p>
  </article>
</section>
```

```js
// Counter animation
const counters = document.querySelectorAll('[data-counter]')
const observer = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const counter = entry.target
      const target = parseInt(counter.dataset.value)
      animateCounter(counter, target)
    }
  })
})

counters.forEach(counter => observer.observe(counter))
```

### With Background

```html
<section data-section="stats" data-variant="dark">
  <div data-stats-grid>
    <!-- Stats with light text -->
  </div>
</section>
```

```css
[data-section="stats"][data-variant="dark"] {
  background: var(--foreground);
  color: var(--background);
}

[data-section="stats"][data-variant="dark"] [data-stat] data {
  color: var(--background);
}
```
