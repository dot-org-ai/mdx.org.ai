---
$type: DocsPage
title: Features Section
description: Feature grid showcasing capabilities
screenshot: /screenshots/sections/features.png
---

# Features Section

A grid of features showcasing your product's capabilities.

<Preview>
  <section data-section="features">
    <header>
      <span>Features</span>
      <h2>Everything you need to build</h2>
      <p>Powerful features to help you ship faster.</p>
    </header>
    <div>
      <article>
        <figure>⚡</figure>
        <h3>Lightning Fast</h3>
        <p>Optimized for performance with zero runtime overhead.</p>
      </article>
      <article>
        <figure>♿</figure>
        <h3>Fully Accessible</h3>
        <p>Built with accessibility in mind from day one.</p>
      </article>
      <article>
        <figure>🔒</figure>
        <h3>Type Safe</h3>
        <p>Full TypeScript support with auto-completion.</p>
      </article>
    </div>
  </section>
</Preview>

## When to Use

- Product feature showcase
- Service offerings
- Capability highlights
- Benefit lists

## Structure

```html
<section data-section="features">
  <header>
    <span>Features</span>
    <h2>Everything you need to build</h2>
    <p>Powerful features to help you ship faster.</p>
  </header>

  <div>
    <article>
      <figure>
        <svg><!-- Lightning icon --></svg>
      </figure>
      <h3>Lightning Fast</h3>
      <p>Optimized for performance with zero runtime overhead.</p>
    </article>

    <article>
      <figure>
        <svg><!-- Accessibility icon --></svg>
      </figure>
      <h3>Fully Accessible</h3>
      <p>Built with accessibility in mind from day one.</p>
    </article>

    <article>
      <figure>
        <svg><!-- Lock icon --></svg>
      </figure>
      <h3>Type Safe</h3>
      <p>Full TypeScript support with auto-completion.</p>
    </article>
  </div>
</section>
```

## CSS

```css
[data-section="features"] {
  padding: var(--spacing-16) var(--spacing-4);
}

[data-section="features"] > header {
  text-align: center;
  margin-bottom: var(--spacing-12);
}

[data-section="features"] > header > span {
  display: inline-block;
  padding: var(--spacing-1) var(--spacing-3);
  background: var(--muted);
  border-radius: var(--radius-full);
  font-size: var(--text-sm);
  margin-bottom: var(--spacing-4);
}

[data-section="features"] > header h2 {
  font-size: var(--text-3xl);
  font-weight: 700;
  margin-bottom: var(--spacing-4);
}

[data-section="features"] > header p {
  color: var(--muted-foreground);
  max-width: 600px;
  margin: 0 auto;
}

[data-section="features"] > div {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spacing-8);
}

[data-section="features"] article {
  text-align: center;
}

[data-section="features"] article figure {
  width: 48px;
  height: 48px;
  background: var(--primary);
  color: var(--primary-foreground);
  border-radius: var(--radius);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto var(--spacing-4);
}

[data-section="features"] article h3 {
  font-weight: 600;
  margin-bottom: var(--spacing-2);
}

[data-section="features"] article p {
  color: var(--muted-foreground);
  font-size: var(--text-sm);
}

@media (max-width: 768px) {
  [data-section="features"] > div {
    grid-template-columns: 1fr;
  }
}
```

## Variations

### With Cards

```html
<section data-section="features">
  <div>
    <article>
      <figure>...</figure>
      <h3>Feature Name</h3>
      <p>Feature description...</p>
      <a href="/features/name">Learn more →</a>
    </article>
  </div>
</section>
```

### Alternating Layout

```html
<section data-section="features">
  <article>
    <div>
      <h3>Feature Name</h3>
      <p>Detailed description...</p>
    </div>
    <figure>
      <img src="/feature-1.png" alt="" />
    </figure>
  </article>
</section>
```
