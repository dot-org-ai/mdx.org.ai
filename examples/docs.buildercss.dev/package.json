{
  "name": "docs.buildercss.dev",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mdxe dev",
    "dev:worker": "wrangler dev",
    "build": "mdxe build",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "test": "vitest run",
    "test:screenshots": "vitest run --config vitest.screenshots.config.ts",
    "screenshots": "npm run test:screenshots -- --update"
  },
  "dependencies": {
    "@mdxe/hono": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@playwright/test": "^1.40.0",
    "vitest": "^2.1.8",
    "wrangler": "^3.90.0"
  }
}
