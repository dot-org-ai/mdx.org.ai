---
$type: DocsPage
title: Fullscreen Container
description: Edge-to-edge immersive experience
screenshot: /screenshots/containers/fullscreen.png
---

# Fullscreen Container

An edge-to-edge container that takes over the entire viewport for immersive experiences.

<Preview>
  <aside aria-modal="true" role="presentation">
    <aside>
      <button>⚙️ Settings</button>
      <button>✕ Exit</button>
    </aside>
    <main>
      <figure>▶</figure>
      <h2>Now Playing</h2>
      <p>Beautiful Sunset - Nature Sounds</p>
    </main>
    <footer>
      <progress value="35" max="100"></progress>
      <nav aria-label="Media controls">
        <button>⏮</button>
        <button aria-current="true">⏸</button>
        <button>⏭</button>
      </nav>
    </footer>
  </aside>
</Preview>

## Semantic Pattern

A fullscreen container uses `<aside aria-modal role="presentation">` for an immersive overlay. The structure includes top controls, main content, and footer controls:

```html
<aside aria-modal="true" role="presentation" aria-hidden="true">
  <aside>
    <button>Settings</button>
    <button>Exit Fullscreen</button>
  </aside>

  <main>
    <video src="/media/video.mp4" autoplay controls></video>
  </main>

  <footer>
    <nav aria-label="Media controls">
      <button>Previous</button>
      <button>Play/Pause</button>
      <button>Next</button>
    </nav>
  </footer>
</aside>
```

## CSS

```css
/* Fullscreen: aside presentation with main content */
aside[aria-modal][role="presentation"]:has(> main) {
  position: fixed;
  inset: 0;
  z-index: 200;
  background: linear-gradient(135deg, #0f0c29 0%, #302b63 50%, #24243e 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
}

aside[aria-modal][role="presentation"]:has(> main) > aside {
  position: absolute;
  top: var(--spacing-4);
  right: var(--spacing-4);
  display: flex;
  gap: var(--spacing-2);
  opacity: 0;
  transition: opacity 0.3s;
}

aside[aria-modal][role="presentation"]:has(> main):hover > aside {
  opacity: 1;
}

aside[aria-modal][role="presentation"]:has(> main) > aside button {
  padding: var(--spacing-2) var(--spacing-3);
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: var(--radius);
  color: white;
  cursor: pointer;
  font-size: var(--text-sm);
}

aside[aria-modal][role="presentation"]:has(> main) > main {
  text-align: center;
}

aside[aria-modal][role="presentation"]:has(> main) > main figure {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  margin: 0 auto var(--spacing-6);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--text-4xl);
}

aside[aria-modal][role="presentation"]:has(> main) > main h2 {
  font-size: var(--text-2xl);
  font-weight: 600;
  margin-bottom: var(--spacing-2);
}

aside[aria-modal][role="presentation"]:has(> main) > main p {
  opacity: 0.7;
}

aside[aria-modal][role="presentation"]:has(> main) > main video,
aside[aria-modal][role="presentation"]:has(> main) > main img {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
}

aside[aria-modal][role="presentation"]:has(> main) > footer {
  position: absolute;
  bottom: var(--spacing-6);
  left: var(--spacing-6);
  right: var(--spacing-6);
  opacity: 0;
  transition: opacity 0.3s;
}

aside[aria-modal][role="presentation"]:has(> main):hover > footer {
  opacity: 1;
}

aside[aria-modal][role="presentation"]:has(> main) > footer progress {
  width: 100%;
  height: 4px;
  margin-bottom: var(--spacing-4);
  background: rgba(255, 255, 255, 0.2);
  border-radius: 2px;
}

aside[aria-modal][role="presentation"]:has(> main) > footer progress::-webkit-progress-bar {
  background: rgba(255, 255, 255, 0.2);
  border-radius: 2px;
}

aside[aria-modal][role="presentation"]:has(> main) > footer progress::-webkit-progress-value {
  background: white;
  border-radius: 2px;
}

aside[aria-modal][role="presentation"]:has(> main) > footer nav {
  display: flex;
  justify-content: center;
  gap: var(--spacing-4);
}

aside[aria-modal][role="presentation"]:has(> main) > footer nav button {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.2);
  border: none;
  color: white;
  cursor: pointer;
}

aside[aria-modal][role="presentation"]:has(> main) > footer nav button[aria-current] {
  width: 56px;
  height: 56px;
  background: white;
  color: black;
}

aside[aria-modal][role="presentation"][aria-hidden="true"] {
  display: none;
}
```

## JavaScript

```js
const fullscreen = document.querySelector('aside[aria-modal][role="presentation"]')

// Enter fullscreen
function enterFullscreen(element) {
  if (element.requestFullscreen) {
    element.requestFullscreen()
  } else if (element.webkitRequestFullscreen) {
    element.webkitRequestFullscreen()
  }
}

// Exit fullscreen
function exitFullscreen() {
  if (document.exitFullscreen) {
    document.exitFullscreen()
  } else if (document.webkitExitFullscreen) {
    document.webkitExitFullscreen()
  }
}

// Exit on Escape
document.addEventListener('keydown', (e) => {
  if (e.key === 'Escape') {
    fullscreen.setAttribute('aria-hidden', 'true')
  }
})

// Auto-hide controls
let hideTimeout
fullscreen.addEventListener('mousemove', () => {
  fullscreen.classList.add('show-controls')
  clearTimeout(hideTimeout)
  hideTimeout = setTimeout(() => {
    fullscreen.classList.remove('show-controls')
  }, 3000)
})
```

## When to Use

- Video players
- Photo galleries
- Media presentations
- Immersive reading
- Games
- Interactive demos
