---
$type: DocsPage
title: Dashboard Layout
description: Full app shell with sidebar and header
screenshot: /screenshots/layouts/dashboard.png
---

# Dashboard Layout

A full application shell with sidebar navigation, header bar, and main content area.

<Preview>
  <div data-layout="dashboard">
    <aside>
      <header>
        <a href="/">Acme Inc</a>
      </header>
      <nav>
        <ul>
          <li><a href="#" aria-current="page">Dashboard</a></li>
          <li><a href="#">Projects</a></li>
          <li><a href="#">Team</a></li>
          <li><a href="#">Settings</a></li>
        </ul>
      </nav>
    </aside>
    <div>
      <header>
        <input type="search" placeholder="Search..." />
        <figure></figure>
      </header>
      <main>
        <h1>Dashboard</h1>
        <section>
          <article>
            <span>Total Users</span>
            <strong>12,345</strong>
          </article>
          <article>
            <span>Revenue</span>
            <strong>$45,678</strong>
          </article>
          <article>
            <span>Active Projects</span>
            <strong>23</strong>
          </article>
        </section>
      </main>
    </div>
  </div>
</Preview>

## When to Use

- SaaS applications
- Admin dashboards
- Internal tools
- Complex web applications

## Structure

```html
<body data-layout="dashboard">
  <aside>
    <header>
      <a href="/">Acme Inc</a>
    </header>
    <nav>
      <ul>
        <li><a href="/dashboard" aria-current="page">Dashboard</a></li>
        <li><a href="/projects">Projects</a></li>
        <li><a href="/team">Team</a></li>
        <li><a href="/settings">Settings</a></li>
      </ul>
    </nav>
  </aside>

  <div>
    <header>
      <input type="search" placeholder="Search..." />
      <figure>
        <img src="/avatar.jpg" alt="User" />
      </figure>
    </header>

    <main>
      <!-- Views go here -->
    </main>
  </div>
</body>
```

## CSS

```css
[data-layout="dashboard"] {
  display: grid;
  grid-template-columns: 280px 1fr;
  min-height: 100vh;
}

[data-layout="dashboard"] > aside {
  background: var(--foreground);
  color: var(--background);
  display: flex;
  flex-direction: column;
  padding: var(--spacing-4);
}

[data-layout="dashboard"] > aside > header a {
  font-weight: 700;
  color: inherit;
}

[data-layout="dashboard"] > aside nav {
  margin-top: var(--spacing-6);
}

[data-layout="dashboard"] > aside ul {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-1);
}

[data-layout="dashboard"] > aside a {
  display: block;
  padding: var(--spacing-2);
  border-radius: var(--radius);
  color: rgba(255, 255, 255, 0.7);
}

[data-layout="dashboard"] > aside a[aria-current] {
  background: rgba(255, 255, 255, 0.1);
  color: inherit;
}

[data-layout="dashboard"] > div {
  display: flex;
  flex-direction: column;
}

[data-layout="dashboard"] > div > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--spacing-4);
  border-bottom: 1px solid var(--border);
}

[data-layout="dashboard"] > div > header input {
  padding: var(--spacing-2);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  width: 300px;
}

[data-layout="dashboard"] > div > header figure {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: var(--muted);
}

[data-layout="dashboard"] > div > main {
  flex: 1;
  padding: var(--spacing-6);
  overflow-y: auto;
}

[data-layout="dashboard"] > div > main section {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spacing-4);
  margin-top: var(--spacing-4);
}

[data-layout="dashboard"] > div > main section article {
  background: var(--muted);
  padding: var(--spacing-4);
  border-radius: var(--radius);
}

[data-layout="dashboard"] > div > main section span {
  font-size: var(--text-sm);
  color: var(--muted-foreground);
}

[data-layout="dashboard"] > div > main section strong {
  display: block;
  font-size: var(--text-2xl);
  margin-top: var(--spacing-1);
}

@media (max-width: 768px) {
  [data-layout="dashboard"] {
    grid-template-columns: 1fr;
  }

  [data-layout="dashboard"] > aside {
    position: fixed;
    inset: 0;
    z-index: 50;
    transform: translateX(-100%);
    transition: transform 0.3s;
  }

  [data-layout="dashboard"][data-sidebar-open] > aside {
    transform: translateX(0);
  }
}
```
