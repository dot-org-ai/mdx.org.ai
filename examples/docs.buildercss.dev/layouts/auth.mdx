---
$type: DocsPage
title: Auth Layout
description: Centered card for login/signup flows
screenshot: /screenshots/layouts/auth.png
---

# Auth Layout

A centered layout optimized for authentication flows like login, signup, and password reset.

<Preview>
  <div data-layout="auth">
    <main>
      <article>
        <header>
          <h1>Sign In</h1>
          <p>Enter your credentials to continue</p>
        </header>
        <form>
          <div>
            <label>Email</label>
            <input type="email" placeholder="you@example.com" />
          </div>
          <div>
            <label>Password</label>
            <input type="password" placeholder="••••••••" />
          </div>
          <button type="submit">Sign In</button>
        </form>
        <footer>
          <p>Don't have an account? <a href="#">Sign up</a></p>
        </footer>
      </article>
    </main>
  </div>
</Preview>

## When to Use

- Login pages
- Registration/signup flows
- Password reset
- Email verification
- Two-factor authentication

## Structure

```html
<body data-layout="auth">
  <main>
    <article>
      <header>
        <img src="/logo.svg" alt="Brand" />
        <h1>Sign In</h1>
        <p>Enter your credentials to continue</p>
      </header>

      <form>
        <div>
          <label for="email">Email</label>
          <input type="email" id="email" required />
        </div>
        <div>
          <label for="password">Password</label>
          <input type="password" id="password" required />
        </div>
        <button type="submit">Sign In</button>
      </form>

      <footer>
        <p>Don't have an account? <a href="/signup">Sign up</a></p>
      </footer>
    </article>
  </main>
</body>
```

## CSS

```css
[data-layout="auth"] {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: var(--muted);
  padding: var(--spacing-4);
}

[data-layout="auth"] > main {
  width: 100%;
  max-width: 420px;
}

[data-layout="auth"] article {
  background: var(--background);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-8);
}

[data-layout="auth"] header {
  text-align: center;
  margin-bottom: var(--spacing-6);
}

[data-layout="auth"] header h1 {
  font-size: var(--text-2xl);
  margin-bottom: var(--spacing-2);
}

[data-layout="auth"] header p {
  color: var(--muted-foreground);
}

[data-layout="auth"] header img {
  height: 40px;
  margin-bottom: var(--spacing-4);
}

[data-layout="auth"] form {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4);
}

[data-layout="auth"] form > div {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-1);
}

[data-layout="auth"] form label {
  font-size: var(--text-sm);
}

[data-layout="auth"] form input {
  width: 100%;
  padding: var(--spacing-2);
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

[data-layout="auth"] form button {
  padding: var(--spacing-2);
  background: var(--primary);
  color: var(--primary-foreground);
  border: none;
  border-radius: var(--radius);
  cursor: pointer;
}

[data-layout="auth"] footer {
  text-align: center;
  margin-top: var(--spacing-4);
  color: var(--muted-foreground);
  font-size: var(--text-sm);
}
```

## React Native

```tsx
import { View, KeyboardAvoidingView } from 'react-native'

export function AuthLayout({ children }) {
  return (
    <KeyboardAvoidingView
      style={styles.container}
      behavior="padding"
    >
      <View style={styles.card}>
        {children}
      </View>
    </KeyboardAvoidingView>
  )
}
```
