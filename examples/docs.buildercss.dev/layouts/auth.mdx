---
$type: DocsPage
title: Auth Layout
description: Centered card for login/signup flows
screenshot: /screenshots/layouts/auth.png
---

# Auth Layout

A centered layout optimized for authentication flows like login, signup, and password reset.

<Preview>
  <div data-layout="auth" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', minHeight: '400px', background: 'var(--muted)', borderRadius: 'var(--radius-lg)' }}>
    <div style={{ background: 'var(--background)', padding: 'var(--spacing-8)', borderRadius: 'var(--radius-lg)', border: '1px solid var(--border)', width: '100%', maxWidth: '400px' }}>
      <header style={{ textAlign: 'center', marginBottom: 'var(--spacing-6)' }}>
        <h1 style={{ fontSize: 'var(--text-2xl)', marginBottom: 'var(--spacing-2)' }}>Sign In</h1>
        <p style={{ color: 'var(--muted-foreground)' }}>Enter your credentials to continue</p>
      </header>
      <form style={{ display: 'flex', flexDirection: 'column', gap: 'var(--spacing-4)' }}>
        <div>
          <label style={{ display: 'block', marginBottom: 'var(--spacing-1)', fontSize: 'var(--text-sm)' }}>Email</label>
          <input type="email" placeholder="you@example.com" style={{ width: '100%', padding: 'var(--spacing-2)', border: '1px solid var(--border)', borderRadius: 'var(--radius)' }} />
        </div>
        <div>
          <label style={{ display: 'block', marginBottom: 'var(--spacing-1)', fontSize: 'var(--text-sm)' }}>Password</label>
          <input type="password" placeholder="••••••••" style={{ width: '100%', padding: 'var(--spacing-2)', border: '1px solid var(--border)', borderRadius: 'var(--radius)' }} />
        </div>
        <button data-button="primary" style={{ padding: 'var(--spacing-2)', background: 'var(--primary)', color: 'var(--primary-foreground)', border: 'none', borderRadius: 'var(--radius)', cursor: 'pointer' }}>Sign In</button>
      </form>
      <footer style={{ textAlign: 'center', marginTop: 'var(--spacing-4)', fontSize: 'var(--text-sm)', color: 'var(--muted-foreground)' }}>
        Don't have an account? <a href="#">Sign up</a>
      </footer>
    </div>
  </div>
</Preview>

## When to Use

- Login pages
- Registration/signup flows
- Password reset
- Email verification
- Two-factor authentication

## Structure

```html
<body data-layout="auth">
  <main>
    <article data-card>
      <header>
        <img src="/logo.svg" alt="Brand" />
        <h1>Sign In</h1>
        <p>Enter your credentials to continue</p>
      </header>

      <form>
        <div data-field>
          <label for="email">Email</label>
          <input type="email" id="email" required />
        </div>
        <div data-field>
          <label for="password">Password</label>
          <input type="password" id="password" required />
        </div>
        <button data-button="primary" type="submit">Sign In</button>
      </form>

      <footer>
        <p>Don't have an account? <a href="/signup">Sign up</a></p>
      </footer>
    </article>
  </main>
</body>
```

## CSS

```css
[data-layout="auth"] {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: var(--muted);
  padding: var(--spacing-4);
}

[data-layout="auth"] > main {
  width: 100%;
  max-width: 420px;
}

[data-layout="auth"] [data-card] {
  background: var(--background);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-8);
}

[data-layout="auth"] header {
  text-align: center;
  margin-bottom: var(--spacing-6);
}

[data-layout="auth"] header img {
  height: 40px;
  margin-bottom: var(--spacing-4);
}

[data-layout="auth"] form {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4);
}

[data-layout="auth"] footer {
  text-align: center;
  margin-top: var(--spacing-4);
  color: var(--muted-foreground);
}
```

## React Native

```tsx
import { View, KeyboardAvoidingView } from 'react-native'

export function AuthLayout({ children }) {
  return (
    <KeyboardAvoidingView
      style={styles.container}
      behavior="padding"
    >
      <View style={styles.card}>
        {children}
      </View>
    </KeyboardAvoidingView>
  )
}
```
